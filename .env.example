# Development: npm run dev reads apps/web/.env.development
# Production build/start: npm run build and npm run start read apps/web/.env.production
# Copy the value below into the environment-specific file you need.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8888/api/v1